apiVersion: v1
kind: Service
metadata:
   name: nginx-deploy-service
spec:
  type: NodePort
  selector: 
      env: dev
  ports:
  - name: nginx-deploy-service-port
    protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30001